import styled from "styled-components";

export const MainMenuWrapper = styled.nav`
  ul {
    display: flex;
    flex-direction: row;
    gap: 10px;

    li {
      list-style-type: none;
    }
  }
`;
