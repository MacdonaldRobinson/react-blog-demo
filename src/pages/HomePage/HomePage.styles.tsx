import styled from "styled-components";

export const HomePageWrapper = styled.section`
  height: 90vh;
`;
export const BlogPostSlidesWrapper = styled.div`
  height: 100%;
  overflow: auto;
  scrollbar-width: thin;
  scroll-behavior: smooth;
  scroll-snap-type: y mandatory;
`;
